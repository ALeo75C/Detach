<div class="block products">
  <div class="pattern">
    <%#= image_tag(asset_path(@imagePath)) %>
  </div>
  <div class="content">
    <h2><%= h2 %></h2>
    <div class="cardsCollection">
      <% cla.all.sample(5).each do |el| %>
      <%= render cardType, mod: el %>
      <% end %>
    </div>
    <%= render "but", rout: rout, mes: mes, method: method %>
  </div>
</div>
