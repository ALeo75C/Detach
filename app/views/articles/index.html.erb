<p id="notice"><%= notice %></p>

<h1>Статьи</h1>

<div class="cardsCollection articles">
  <% @articles.each do |art| %>
    <%= render "articleCard", mod: art %>
  <% end %>
</div>

<% if can? :edit, @post %>
  <%= render "but", mes: "Новая статья", rout: new_article_path, method: :get %>
<% end %>
