<%= form_with(model: active_effect, local: true) do |form| %>
  <% if active_effect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(active_effect.errors.count, "error") %> prohibited this active_effect from being saved:</h2>

      <ul>
        <% active_effect.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
