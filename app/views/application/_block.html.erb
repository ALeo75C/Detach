<div class="block">
  <h2><%= @h2 %></h2>
  <div class="cardsCollection">
    <% @cla.all.sample(5).each do |el| %>
    <% @mod = el %>
    <%= render @cardType %>
    <% end %>
  </div>
  <%= render "but" %>
</div>
